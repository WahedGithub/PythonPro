def avg( var1, var2, var3 ):
   total= var1+ var2+ var3
   avg= total/3
   return avg

ret = avg(10,20,30) # If return statement is used than ret value should be assigned to the function name and also need to call for a print even after function is defined
print ("The average value is:", ret)

ret = avg(20,40,80) # If return statement is used than ret value should be assigned to the function name and also need to call for a print even after function is defined
print ("The average value is:", ret)

