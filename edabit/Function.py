# Function definition is here
def increment( n ):

   for n in r(n,20):
	n =+1
	print(n)
   return;

# Now you can call printme function
increment(n) # whatever we are assigning to the str is a required argument without which program will throw error
